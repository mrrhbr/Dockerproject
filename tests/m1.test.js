const { add, subtract, devide, multiply } = require("../math.js");
describe("Math. js tests", () => {
  // it("should test add ",()=>{
  //   expect (add (2, 2)). toBe (4) ;
  // });
  // it("should test subtract",()=>{
  //   expect (subtract (2, 2)). toBe (0) ;
  // });

  // it("should test devide",()=>{
  //   expect (devide (2, 2)). toBe (1) ;
  // });
  // it("should test mulitply",()=>{
  //   expect (multiply (2, 2)). toBe (4) ;
  // });
  // it("should test add ",()=>{
  //   expect (add (15, 3)). toBe (18) ;
  // });
  // it("should test subtract",()=>{
  //   expect (subtract (15, 3)). toBe (12) ;
  // });

  // it("should test devide",()=>{
  //   expect (devide (15, 3)). toBe (5) ;
  // });
  // it("should test mulitply",()=>{
  //   expect (multiply (15, 3)). toBe (45) ;
  // });
  // it("should test add ",()=>{
  //   expect (add (20, 4)). toBe (24) ;
  // });
  // it("should test subtract",()=>{
  //   expect (subtract (20, 4)). toBe (16) ;
  // });

  // it("should test devide",()=>{
  //   expect (devide (20, 4)). toBe (5) ;
  // });
  it("should test mulitply",()=>{
    expect (multiply (20, 4)). toBe (80) ;
  });
});